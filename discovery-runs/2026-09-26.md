# Discovery run 2026-09-26

Scope: daily askhermie.dev autonomous discovery loop.

## Accepted and published

- Gemini CLI — inspected `google-gemini/gemini-cli` via GitHub API and README. Apache-2.0, active, high reputation. Safety note: terminal agents can read files, run commands, and use MCP tools; publish only with sandbox/diff-review guidance.
- AgentTeams — inspected `agentscope-ai/AgentTeams` via GitHub API and README. Apache-2.0, active, transparent human-in-the-loop multi-agent coordination. Safety note: multi-agent workspaces can amplify mistakes and tool access; publish only with sandbox/test-account guidance.
- ESP32 RF Lab — inspected `Vivek-live-now/ESP32-RF-Lab` via GitHub API and README. README states MIT, active, measurement-focused ESP32 Wi-Fi/RF lab. Safety note: use only owned boards and networks; do not scan or stress third-party networks.
- Gitleaks — inspected `gitleaks/gitleaks` via GitHub API and README. MIT, defensive secret scanning, active security-patch posture. Safety note: findings may contain real secrets; keep output private and rotate credentials.
- OSV-Scanner — inspected `google/osv-scanner` via GitHub API and README. Apache-2.0, Google-supported defensive dependency scanning. Safety note: vulnerability reports can reveal private dependencies and project structure.

## Already present / inspected

- Wokwi Elements — already in catalog as `Wokwi Elements`; re-inspected `wokwi/wokwi-elements`, benign visual electronics components.
- ESP-Brookesia — already in catalog as `ESP-Brookesia`; re-inspected `espressif/esp-brookesia`, benign ESP-IDF HMI framework.
- MCUboot — already in catalog as `MCUboot`; re-inspected `mcu-tools/mcuboot`, benign embedded secure bootloader with bricking caution.
- Sealed Secrets — already in catalog as `Sealed Secrets`; re-inspected `bitnami/sealed-secrets`, defensive Kubernetes secret encryption.
- ESP32 Bit Pirate — already in catalog as `ESP32 Bit Pirate`; re-inspected `geo-tp/ESP32-Bit-Pirate`, dual-use hardware protocol lab already framed with owned-hardware caution.
- mise — already in catalog as `mise`; re-inspected `jdx/mise`, benign developer tool/env/task runner.

## Rejected or deferred

- Drosera / `lentago/homelab-observability` — deferred. It is an active MIT observability repo, but appears tailored to one lab/cloud estate and is less beginner-reusable than generic stacks already in the catalog.
- `gavxm/observability-homelab` — deferred. Benign MIT Docker Compose homelab stack, but zero-star, personal-server-specific, and overlaps existing observability/homelab entries.
- 1Panel / `1Panel-dev/1Panel` — deferred. Popular and probably useful, but the primary install path for server management panels is high-impact on Linux hosts; needs a separate safety pass before recommending to beginners.

## Skills

No local Hermes skills created or updated. None of the accepted entries needed a new reusable Hermes workflow beyond a beginner resource card.
